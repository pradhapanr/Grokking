Completed problems in Grokking the Coding Interview.

Each problem has test cases. If you have `pytest` installed, you should be able to 
run the following command to compile vs. the test cases:

`pytest <program_name>`